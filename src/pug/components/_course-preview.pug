mixin course-preview(item)
  div(class= item.cl ? item.cl + ' course-preview' : 'course-preview')
    if item.isLabel
      span.course-preview__label
        | Акция %
        //-+icon('info')(width="20", height="20")
    .course-preview__image
      img(src="images/course-preview/" + item.img.n + ".jpg", srcset="images/course-preview/" + item.img.n + "@2x.jpg 2x", width= item.img.w, height= item.img.h, alt="")
    .course-preview__content
      h3.course-preview__title!= item.title
      .course-preview__list
        each text in item.list
          span= text
      .course-preview__text!= item.text
      .course-preview__action 
        +button('Подробнее', 'course-preview__btn button--white button--more', 'chevron-right', URLS.course)
        .course-preview__price
          if item.price.old
            .course-preview__price-old от 55 000₽
          | #{item.price.current}
      a.course-preview__link(href="#") Приобрести в подарок